# Protect runtime data from direct web access.
# Keep this directory readable only by backend PHP via filesystem paths.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order Allow,Deny
    Deny from all
</IfModule>
