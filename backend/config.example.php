<?php
/**
 * Legacy fallback: copy this file to `config.php` in this directory only if you do not use
 * `config/config.php`. Recommended: copy `config/config.example.php` to `config/config.php`.
 *
 * IMPORTANT: Never commit `config.php` or `config/config.php` — they contain secrets.
 */
require_once __DIR__ . '/config/config.example.php';
