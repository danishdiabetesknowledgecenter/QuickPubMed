# Apache .htaccess for API-specific CORS fallback

<IfModule mod_headers.c>
    # Non-editor endpoints set CORS in PHP (origin allowlist via config).
    # Avoid wildcard fallback here to reduce cross-origin exposure.

    # Editor endpoints set CORS in PHP via the configured origin allowlist.
    # Do not reflect arbitrary origins at the Apache layer.
</IfModule>
